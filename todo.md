# Phase 4 — Categorize + translate (rule-based)

- [ ] Build a Python dictionary mapping each of 238 Chinese names → (category, name_en, name_th)
- [ ] Run script to produce three columns (category, name_en, name_th)
- [ ] Push results into the verified Google Sheet for Disk's spot-check
- [ ] After Disk approves, freeze the data into a JSON file in the project for Phase 5+ use
